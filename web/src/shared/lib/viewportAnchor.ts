/** Viewport coordinates used to anchor pointer-triggered overlays. */
export type ViewportAnchorPoint = {
  /** Horizontal viewport coordinate in CSS pixels. */
  x: number
  /** Vertical viewport coordinate in CSS pixels. */
  y: number
}
